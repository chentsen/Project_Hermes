<div id="view-content">
        <div class="view-wrapped">
		<h1>Events I'm Attending</h1>
		<?php $auth = Zend_Auth::getInstance(); 
                    if($auth->hasIdentity()){ 
                    	$this->identity = $auth->getIdentity()?>
		 
		 <?= $this->DisplayEventFeedPage($this->identity); ?>
		  <?php } ?>
	</div>
</div>